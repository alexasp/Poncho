<Window x:Class="Poncho.Views.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SearchView" Height="136" Width="395">
    <Grid Height="100" Width="360">
        <Grid Margin="12,12,0,0" Name="grid1" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="35" />
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Height="35" Name="textBox1" VerticalAlignment="Top" Width="Auto" />
            <Button Grid.Row="1" Content="Search" Margin="5,5,5,5" HorizontalAlignment="Right" Name="searchButton" Width="75" UseLayoutRounding="False" />
        </Grid>
    </Grid>
</Window>
